class Asignados_Tareas {
    constructor(id, task_id, user_id, assigned_on, assigned_by) {
        this.id = id;
        this.task_id = task_id;
        this.user_id = user_id;
        this.assigned_on = assigned_on;
        this.assigned_by = assigned_by;
    }
}